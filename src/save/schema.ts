/**
 * Save schema entry point. Re-exports the SaveStateV2 type from the central
 * types module so the save layer has a single import surface.
 */
export type { SaveStateV2 } from '../types/Save.js';
