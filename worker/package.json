{
  "name": "fishtank-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker serving the fishtank app at mccarrison.me/fish",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
